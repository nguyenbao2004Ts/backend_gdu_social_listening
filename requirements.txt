# Python 3.12+ recommended (see docs/gdu-social-listening-plan.md)
# Install: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Prisma ORM (Python client + CLI)
prisma==0.15.0

# Auth (Module 1)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Dev / quality (optional)
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.24.0
